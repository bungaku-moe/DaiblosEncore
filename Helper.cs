namespace DaiblosEncore;

public static class Helper
{
    // public static TextAsset CreateTextAsset(byte[] rawBytes)
    // {
    //     // var il2cppArray = new Il2CppStructArray<byte>(rawBytes.Length);
    //     // for (int i = 0; i < rawBytes.Length; i++)
    //     //     il2cppArray[i] = rawBytes[i];
    //     TextAsset asset = new();
    //     FieldInfo? textField = typeof(TextAsset)
    //         .GetField("text", BindingFlags.NonPublic | BindingFlags.Instance | Binding);
    //
    //     textField.SetValue(asset, il2cppArray);
    //
    //     return asset;
    // }
}